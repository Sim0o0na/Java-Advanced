package com.company;

import java.text.MessageFormat;
import java.util.Scanner;

/**
 * Created by Simona Simeonova on 18-Jan-17.
 */
public class CalculateExpression {
    public static void main(String[] args) {
//        Scanner scan = new Scanner(System.in);
//        double a = scan.nextDouble();
//        double b = scan.nextDouble();
//        double c = scan.nextDouble();
//
//        double insideExpr = (Math.pow(a, 2) + (Math.pow(b, 2))/((Math.pow(a, 2) - (Math.pow(b, 2)))));
//        double expr = Math.pow(()), (a+b+c)/Math.sqrt(c));
//        double expr2 = Math.pow((Math.pow(a,2) + Math.pow(b,2) - Math.pow(c,3)), a-b);
//        double exprAvg = (expr + expr2)/2;
//        double avg = (a+b+c)/3;
//
//        System.out.print(MessageFormat.format("Expr1: {0} Expr2: {1} Avg {2}", expr,expr2,avg));
    }
}
